# AI Lab: server-side settings only; never use a VITE_ prefix for secrets.
# Enable only after configuring provider spending limits and hosting rate limits.
AI_DEMO_ENABLED=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
